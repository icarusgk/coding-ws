import toml
import pprint
import yaml



# data = toml.load('data.toml')
# with open('data.yml', 'r') as file:
#   data = yaml.safe_load(file)
#   pprint.pp(data["author"]["courses"], compact=False, width=100)

